apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  namespace: naftiko-warden
  name: ${{ values.name }}-api
  description: ${{ values.name }} API
spec:
  type: openapi
  lifecycle: experimental
  owner: user:guest
  definition:
    $text: ./openapi.yaml